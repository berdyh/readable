# server.llm.providers

> Adapts each upstream model API to one provider interface.

<!-- Generated by scripts/modules/generate-agents.mjs from module.manifest.json
     and module.narrative.md. Edit those, not this file. -->

`local-coding-agent.ts` is the odd one: it shells out to a local CLI (Codex,
Claude Code) rather than calling an HTTP API, running it in an isolated temp `HOME`/`TMPDIR`
with app secrets stripped from the environment. Because that also hides the agent's own
subscription credential, each invocation stages just that one file into the sandbox — never
the whole config tree, and never Claude's third-party MCP tokens. Treat changes there as
security-relevant.

## Interface

Entrypoint: `(via ../index.ts)`

Anything not listed here is internal to this module:

- `OpenAiProvider`
- `AnthropicProvider`
- `GeminiProvider`
- `OpenRouterProvider`
- `LocalCodingAgentProvider`

## Uses

Read only the *interface* of these — never their internals.

- **server.config** via `getTimeout`
- **server.llm-config** via `getModel`

## Constraints

Work outside these has left this module. Stop and ask rather than widening the boundary.

- one class per upstream API
- request/response translation

## Checks

Green here means the bounded work is done — no need to run the whole repo.

```bash
pnpm vitest run src/server/llm/providers
```
