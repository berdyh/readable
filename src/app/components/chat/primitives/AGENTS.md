# web.chat.primitives

> Renders chat markup from props.

<!-- Generated by scripts/modules/generate-agents.mjs from module.manifest.json
     and module.narrative.md. Edit those, not this file. -->

May not fetch or reach app state. `answer-card.tsx` carries the trust strip —
the visual difference between a sourced answer, an uncited one, and a refusal.

## Interface

Entrypoint: `(imported by ../sidecar and ../inline)`

Anything not listed here is internal to this module:

- `AnswerCard`
- `Conversation`
- `LocalAgentPicker`
- `Message`
- `PromptInputTextarea`
- `Source`

## Constraints

Work outside these has left this module. Stop and ask rather than widening the boundary.

- props to markup

## Checks

Green here means the bounded work is done — no need to run the whole repo.

```bash
pnpm vitest run src/app/components/chat
```
