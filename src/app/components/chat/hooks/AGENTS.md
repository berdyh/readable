# web.chat.hooks

> Holds the chat sidecar's client state.

<!-- Generated by scripts/modules/generate-agents.mjs from module.manifest.json
     and module.narrative.md. Edit those, not this file. -->

May not style the DOM directly. Effects belong here; measurement and scroll
behaviour that must touch nodes belongs in the component that owns them.

## Interface

Entrypoint: `(imported by ../sidecar and ../inline)`

Anything not listed here is internal to this module:

- `useChatSessions`
- `useChatPanelWidth`
- `useEditorIntentPrompt`
- `useLocalAgents`
- `useSlashCommandMenu`

## Uses

Read only the *interface* of these — never their internals.

- **server.qa** via `@/server/qa/types`

## Constraints

Work outside these has left this module. Stop and ask rather than widening the boundary.

- client state
- effects

## Checks

Green here means the bounded work is done — no need to run the whole repo.

```bash
pnpm vitest run src/app/components/chat
```
